import { BgColorActiveDirective } from './bg-color-active.directive';

describe('BgColorActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BgColorActiveDirective();
    expect(directive).toBeTruthy();
  });
});
